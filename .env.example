# Selects the non-production runtime environment.
NODE_ENV=development

# This MVP is deliberately restricted to paper trading.
TRADING_MODE=paper

# Add Supabase connection strings locally; never commit credentials.
DATABASE_NAME=trading-bot-dev
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
